# Инструкция по исправлению ошибок регистрации

## Проблема
При регистрации новых пользователей возникает ошибка "Database error saving new user". Это вызвано проблемами в триггере базы данных, который автоматически создает профиль пользователя при регистрации.

## Решение
Создана новая миграция `fix_user_creation_trigger.sql`, которая полностью переписывает подход к созданию профиля пользователя с более надежной обработкой ошибок.

## Применение миграции

1. Войдите в панель управления Supabase по адресу: https://supabase.com/dashboard
2. Выберите ваш проект (wrfcpsljchyetbmupqgc)
3. Перейдите в раздел "SQL Editor"
4. Выполните SQL-запрос из файла `database/migrations/fix_user_creation_trigger.sql`

## Альтернативный способ (через командную строку)

Если у вас установлен Supabase CLI, выполните команду:
```bash
supabase db push
```

## Что изменилось

1. **Обновленная функция создания профиля с полем role**:
   ```sql
   CREATE OR REPLACE FUNCTION public.handle_new_user()
   RETURNS trigger AS $$
   BEGIN
     -- Проверяем, существует ли уже профиль с таким ID
     IF NOT EXISTS (SELECT 1 FROM profiles WHERE id = new.id) THEN
       INSERT INTO public.profiles (id, full_name, role)
       VALUES (
         new.id,
         COALESCE(new.raw_user_meta_data->>'full_name', ''),
         'user'
       );
     END IF;
     RETURN new;
   END;
   $$ LANGUAGE plpgsql SECURITY DEFINER;
   ```

   Эта версия функции:
   - Проверяет, существует ли уже профиль с таким ID, чтобы избежать дублирования
   - Вставляет значение 'user' в поле role для всех новых пользователей
   - Любая ошибка прервет транзакцию регистрации, обеспечивая целостность данных

2. **Добавление поля role в таблицу profiles**:
   ```sql
   CREATE TABLE profiles (
     id UUID REFERENCES auth.users(id) PRIMARY KEY,
     full_name TEXT,
     role VARCHAR(20) NOT NULL DEFAULT 'user',
     avatar_url TEXT,
     city TEXT,
     interests TEXT,
     created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
     updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
   );
   ```

3. **Обновление разрешений**:
   ```sql
   GRANT EXECUTE ON FUNCTION public.handle_new_user() TO service_role;
   ```

   Разрешения предоставлены только для service_role, так как функция вызывается только триггером.

4. **Использование CREATE OR REPLACE POLICY**:
   ```sql
   CREATE OR REPLACE POLICY "Пользователи могут создавать свой профиль" ON profiles
     FOR INSERT WITH CHECK (auth.uid() = id);
   ```

   Это избегает ненужного удаления и повторного создания политики.

## Почему предыдущий подход вызывал проблемы

Предыдущая версия функции имела блок `EXCEPTION WHEN OTHERS`, который перехватывал все ошибки и позволял регистрации продолжаться даже при сбое создания профиля. Это приводило к:

1. Созданию пользователей в auth.users без соответствующих записей в profiles
2. Нарушению целостности данных между таблицами
3. Сложностям при отладке, так как ошибки только логировались, но не были видны пользователям

## Поведение после исправления

Теперь при любой ошибке при создании профиля:
1. Вся транзакция регистрации будет отменена
2. Пользователь получит сообщение об ошибке
3. Не будет создана частичная запись пользователя без профиля
4. Все RLS политики, использующие поле role, будут работать корректно
5. Для пользователей, зарегистрированных до исправления, требуется ручное создание профилей (см. раздел 'Восстановление профилей')

После применения этой миграции регистрация новых пользователей будет работать надежно с полной целостностью данных.

## Восстановление профилей для существующих пользователей

### Проблема

Пользователи, зарегистрированные между [датой применения первого исправления] и [датой применения финального исправления], могут не иметь профилей в таблице `profiles`. Это произошло из-за того, что триггер проглатывал ошибки через блок `EXCEPTION WHEN OTHERS`.

### Выявленные пользователи без профилей

- `acd70956-59ff-45d7-ba63-96688c84aa40` (andrei_999@list.ru) - зарегистрирован 31.10.2025

### Применение миграции восстановления

1. Войдите в панель управления Supabase: https://supabase.com/dashboard
2. Выберите проект (wrfcpsljchyetbmupqgc)
3. Перейдите в раздел "SQL Editor"
4. Выполните SQL-запрос из файла `database/migrations/create_missing_profile.sql`
5. Проверьте результат выполнения - должно появиться сообщение "Profile created successfully"

### Проверка результата

Выполните следующий запрос для проверки что профиль создан:

```sql
SELECT id, full_name, role, created_at
FROM profiles
WHERE id = 'acd70956-59ff-45d7-ba63-96688c84aa40';
```

Ожидаемый результат:
- `id`: acd70956-59ff-45d7-ba63-96688c84aa40
- `full_name`: '' (пустая строка)
- `role`: 'user'
- `created_at`: текущая дата/время выполнения миграции

### Поиск других пользователей без профилей

Для проверки наличия других пользователей без профилей выполните:

```sql
SELECT
  au.id,
  au.email,
  au.created_at,
  CASE WHEN p.id IS NULL THEN 'НЕТ ПРОФИЛЯ' ELSE 'ЕСТЬ ПРОФИЛЬ' END as status
FROM auth.users au
LEFT JOIN public.profiles p ON au.id = p.id
WHERE p.id IS NULL
ORDER BY au.created_at DESC;
```

Если запрос возвращает пользователей, создайте для них профили аналогичным образом, скопировав структуру из `create_missing_profile.sql` и заменив UUID.

### Последствия восстановления

После создания профиля пользователь сможет:
- Открыть страницу профиля без ошибок 406 Not Acceptable
- Присоединяться к событиям без ошибок 409 Conflict (FK constraint)
- Использовать все функции системы, требующие наличия профиля

## Комплексное тестирование исправлений

### Обзор тестирования

После применения всех исправлений необходимо выполнить комплексное тестирование для проверки:
1. Корректности работы триггера создания профилей
2. Восстановления профиля для пользователя acd70956-59ff-45d7-ba63-96688c84aa40
3. Отсутствия 406 ошибок при запросах к profiles
4. Отсутствия 409 ошибок при присоединении к событиям
5. Целостности данных между auth.users и profiles

### Тест 1: Регистрация нового пользователя

**Цель**: Проверить что триггер `handle_new_user()` создает профиль с колонкой `role='user'`

**Предварительные условия**:
- Миграция `fix_user_creation_trigger.sql` применена
- Доступ к Supabase SQL Editor

**Шаги выполнения**:
1. Откройте приложение в браузере
2. Перейдите на страницу регистрации `/register`
3. Зарегистрируйте нового пользователя:
   - Email: `test_$(timestamp)@test.com` (используйте уникальный email)
   - Пароль: минимум 6 символов
   - Имя: "Test User"
4. Дождитесь успешной регистрации и автоматического входа
5. Откройте Supabase Dashboard → SQL Editor
6. Выполните проверочный запрос:
   ```sql
   SELECT
     p.id,
     p.full_name,
     p.role,
     p.created_at,
     au.email
   FROM profiles p
   JOIN auth.users au ON p.id = au.id
   WHERE au.email = 'test_$(timestamp)@test.com';
   ```

**Ожидаемый результат**:
- ✅ Запрос возвращает 1 строку
- ✅ Колонка `role` имеет значение `'user'`
- ✅ Колонка `full_name` содержит "Test User" или пустую строку
- ✅ Колонка `created_at` заполнена текущей датой
- ✅ В консоли браузера нет ошибок
- ✅ Нет ошибок "Database error saving new user"

**Критерий провала**:
- ❌ Запрос возвращает 0 строк (профиль не создан)
- ❌ Колонка `role` NULL или отсутствует
- ❌ Ошибка в консоли браузера
- ❌ Ошибка регистрации в приложении

**Действия при провале**:
1. Проверьте что миграция `fix_user_creation_trigger.sql` применена
2. Проверьте логи Supabase: Dashboard → Logs → Database
3. Выполните запрос для проверки триггера:
   ```sql
   SELECT trigger_name, event_manipulation, action_statement
   FROM information_schema.triggers
   WHERE trigger_name = 'on_auth_user_created';
   ```
4. Проверьте код функции:
   ```sql
   SELECT pg_get_functiondef('public.handle_new_user'::regproc);
   ```

---

### Тест 2: Страница профиля восстановленного пользователя

**Цель**: Проверить что пользователь `acd70956-59ff-45d7-ba63-96688c84aa40` может открыть профиль без 406 ошибок

**Предварительные условия**:
- Миграция `create_missing_profile.sql` применена
- Замена `.single()` на `.maybeSingle()` в `Profile.jsx` выполнена
- Доступ к учётной записи `andrei_999@list.ru`

**Шаги выполнения**:
1. Откройте приложение в режиме инкогнито (Ctrl+Shift+N)
2. Войдите под пользователем `andrei_999@list.ru`
3. Откройте DevTools (F12) → вкладка Console
4. Откройте DevTools → вкладка Network
5. Перейдите на страницу `/profile`
6. Дождитесь полной загрузки страницы
7. Проверьте вкладку Network:
   - Найдите запрос `GET /rest/v1/profiles?select=*&id=eq.acd70956...`
   - Проверьте статус ответа
8. Проверьте вкладку Console на наличие ошибок

**Ожидаемый результат**:
- ✅ Страница профиля загружается полностью
- ✅ Компонент `GameCard` отображается с данными пользователя
- ✅ Запрос к `/rest/v1/profiles` возвращает статус 200 OK
- ✅ В консоли нет ошибок `406 Not Acceptable`
- ✅ В консоли нет сообщений "Профиль не найден, создаём пустой объект"
- ✅ Табы профиля (Профиль, Приглашения, Друзья и т.д.) работают

**Критерий провала**:
- ❌ Ошибка `406 Not Acceptable` в Network
- ❌ Сообщение "Профиль не найден" в Console
- ❌ Страница не загружается или показывает ошибку
- ❌ Компонент `GameCard` не отображается

**Действия при провале**:
1. Проверьте что миграция `create_missing_profile.sql` применена:
   ```sql
   SELECT id, full_name, role, created_at
   FROM profiles
   WHERE id = 'acd70956-59ff-45d7-ba63-96688c84aa40';
   ```
2. Если профиль отсутствует, примените миграцию вручную
3. Проверьте код `Profile.jsx` строки 46-50: должен использоваться `.maybeSingle()`
4. Проверьте обработку `!data` на строках 62-71

---

### Тест 3: Присоединение к событию

**Цель**: Проверить что пользователь может присоединиться к событию без 409 ошибок FK constraint

**Предварительные условия**:
- Профиль пользователя `acd70956-59ff-45d7-ba63-96688c84aa40` создан
- Существует хотя бы одно активное событие с доступными местами
- Пользователь не является участником этого события

**Шаги выполнения**:
1. Войдите под пользователем `andrei_999@list.ru`
2. Откройте DevTools (F12) → вкладки Console и Network
3. Перейдите на страницу `/events`
4. Выберите любое событие с доступными местами (не полное, не отменённое)
5. Откройте страницу события `/events/[event_id]`
6. Нажмите кнопку "Присоединиться к событию"
7. Дождитесь изменения кнопки на "Покинуть событие"
8. Проверьте вкладку Network:
   - Найдите запрос `POST /rest/v1/event_participants`
   - Проверьте статус ответа и тело ответа
9. Проверьте вкладку Console на наличие ошибок
10. Перейдите на вкладку "Участники" и проверьте что вы в списке

**Ожидаемый результат**:
- ✅ Кнопка меняется с "Присоединиться" на "Покинуть событие"
- ✅ Запрос `POST /rest/v1/event_participants` возвращает 201 Created
- ✅ В консоли нет ошибок `409 Conflict`
- ✅ В консоли нет сообщений о нарушении FK constraint `event_participants_user_id_fkey`
- ✅ Пользователь появляется в списке участников на вкладке "Участники"
- ✅ Счётчик участников увеличивается (например, с 3/10 на 4/10)

**Критерий провала**:
- ❌ Ошибка `409 Conflict` в Network
- ❌ Сообщение о нарушении FK constraint в Console
- ❌ Кнопка не меняется или показывает ошибку
- ❌ Пользователь не появляется в списке участников

**Действия при провале**:
1. Проверьте наличие профиля:
   ```sql
   SELECT id, role FROM profiles WHERE id = 'acd70956-59ff-45d7-ba63-96688c84aa40';
   ```
2. Проверьте FK constraint:
   ```sql
   SELECT conname, contype, confrelid::regclass
   FROM pg_constraint
   WHERE conrelid = 'event_participants'::regclass
     AND conname = 'event_participants_user_id_fkey';
   ```
3. Попробуйте вручную вставить запись:
   ```sql
   INSERT INTO event_participants (event_id, user_id, status)
   VALUES ('[event_id]', 'acd70956-59ff-45d7-ba63-96688c84aa40', 'confirmed');
   ```
4. Если ошибка повторяется, проверьте что профиль действительно существует

---

### Тест 4: Доступ к админ-панели

**Цель**: Проверить что модераторы получают доступ к `/admin`, а обычные пользователи - нет, без 406 ошибок

**Предварительные условия**:
- Замена `.single()` на `.maybeSingle()` в `Admin.jsx`, `TopNav.jsx`, `ProtectedAdminRoute.jsx` выполнена
- Существует пользователь с ролью `moderator`
- Существует пользователь с ролью `user`

**Подготовка тестовых данных**:
1. Создайте тестового модератора (если нет):
   ```sql
   -- Найдите существующего пользователя или создайте нового
   UPDATE profiles
   SET role = 'moderator'
   WHERE id = (SELECT id FROM auth.users WHERE email = 'test_moderator@test.com');
   ```

**Шаги выполнения (для модератора)**:
1. Войдите под пользователем с ролью `moderator`
2. Откройте DevTools (F12) → вкладки Console и Network
3. Перейдите на страницу `/admin`
4. Проверьте вкладку Network:
   - Найдите запрос `GET /rest/v1/profiles?select=role&id=eq...`
   - Проверьте статус ответа
5. Проверьте вкладку Console на наличие ошибок
6. Проверьте что отображаются вкладки: "Жалобы", "Пользователи", "Обжалования"
7. Проверьте что в навигации `TopNav` отображается пункт "Админ"

**Ожидаемый результат (модератор)**:
- ✅ Страница `/admin` загружается
- ✅ Запрос к `/rest/v1/profiles?select=role` возвращает 200 OK с `role='moderator'`
- ✅ В консоли нет ошибок `406 Not Acceptable`
- ✅ Отображаются все вкладки админ-панели
- ✅ В навигации есть пункт "Админ" (иконка ⚙️)

**Шаги выполнения (для обычного пользователя)**:
1. Выйдите из аккаунта модератора
2. Войдите под пользователем с ролью `user` (например, `andrei_999@list.ru`)
3. Откройте DevTools (F12) → вкладки Console и Network
4. Попытайтесь перейти на страницу `/admin` (введите URL вручную)
5. Проверьте что произошёл редирект на главную `/`
6. Проверьте вкладку Console на наличие ошибок
7. Проверьте что в навигации `TopNav` НЕТ пункта "Админ"

**Ожидаемый результат (обычный пользователь)**:
- ✅ Редирект на главную страницу `/`
- ✅ Запрос к `/rest/v1/profiles?select=role` возвращает 200 OK с `role='user'`
- ✅ В консоли нет ошибок `406 Not Acceptable`
- ✅ В консоли может быть предупреждение "Профиль не найден" (допустимо)
- ✅ В навигации НЕТ пункта "Админ"

**Критерий провала**:
- ❌ Ошибка `406 Not Acceptable` для любого пользователя
- ❌ Обычный пользователь получает доступ к админ-панели
- ❌ Модератор не получает доступ к админ-панели
- ❌ Страница не загружается или показывает ошибку

**Действия при провале**:
1. Проверьте код `Admin.jsx` строки 28-32: должен использоваться `.maybeSingle()`
2. Проверьте условие на строке 42: `if (!profile?.role || profile.role !== 'moderator')`
3. Проверьте код `TopNav.jsx` строки 28-33: должен использоваться `.maybeSingle()`
4. Проверьте код `ProtectedAdminRoute.jsx` строки 24-28: должен использоваться `.maybeSingle()`
5. Проверьте роль пользователя:
   ```sql
   SELECT id, email, role FROM profiles p
   JOIN auth.users au ON p.id = au.id
   WHERE au.email = 'test_moderator@test.com';
   ```

---

### Тест 5: Целостность данных

**Цель**: Убедиться что все пользователи в системе имеют профили и все профили имеют заполненную колонку `role`

**Предварительные условия**:
- Доступ к Supabase SQL Editor
- Все предыдущие миграции применены

**Шаги выполнения**:
1. Откройте Supabase Dashboard → SQL Editor
2. Выполните запрос для поиска пользователей без профилей:
   ```sql
   SELECT
     au.id,
     au.email,
     au.created_at,
     CASE WHEN p.id IS NULL THEN '❌ НЕТ ПРОФИЛЯ' ELSE '✅ ЕСТЬ ПРОФИЛЬ' END as status,
     p.role,
     p.full_name
   FROM auth.users au
   LEFT JOIN public.profiles p ON au.id = p.id
   ORDER BY au.created_at DESC;
   ```
3. Проанализируйте результаты:
   - Подсчитайте количество пользователей со статусом "❌ НЕТ ПРОФИЛЯ"
   - Проверьте что у всех профилей заполнена колонка `role`
4. Выполните запрос для подсчёта пользователей без профилей:
   ```sql
   SELECT COUNT(*) as users_without_profiles
   FROM auth.users au
   LEFT JOIN public.profiles p ON au.id = p.id
   WHERE p.id IS NULL;
   ```
5. Выполните запрос для проверки профилей без роли:
   ```sql
   SELECT COUNT(*) as profiles_without_role
   FROM profiles
   WHERE role IS NULL OR role = '';
   ```

**Ожидаемый результат**:
- ✅ Все пользователи имеют статус "✅ ЕСТЬ ПРОФИЛЬ"
- ✅ Запрос `users_without_profiles` возвращает 0
- ✅ Запрос `profiles_without_role` возвращает 0
- ✅ Пользователь `acd70956-59ff-45d7-ba63-96688c84aa40` присутствует с профилем
- ✅ Все профили имеют `role` = 'user', 'moderator' или 'admin'

**Критерий провала**:
- ❌ Найдены пользователи без профилей (count > 0)
- ❌ Найдены профили без роли (count > 0)
- ❌ Пользователь `acd70956-59ff-45d7-ba63-96688c84aa40` отсутствует

**Действия при провале**:
1. Если найдены пользователи без профилей:
   - Скопируйте их UUID из результатов запроса
   - Для каждого UUID создайте профиль используя шаблон из `create_missing_profile.sql`:
     ```sql
     DO $
     BEGIN
       IF NOT EXISTS (SELECT 1 FROM profiles WHERE id = '[UUID]') THEN
         INSERT INTO public.profiles (id, full_name, role)
         VALUES ('[UUID]'::uuid, '', 'user');
         RAISE NOTICE 'Profile created for user %', '[UUID]';
       END IF;
     END $;
     ```
   - Повторите проверку

2. Если найдены профили без роли:
   - Обновите их роль на 'user':
     ```sql
     UPDATE profiles
     SET role = 'user'
     WHERE role IS NULL OR role = '';
     ```
   - Повторите проверку

---

### Дополнительные проверки

#### Проверка логов Supabase

1. Откройте Supabase Dashboard → Logs → Database
2. Установите фильтр времени: Last 24 hours
3. Найдите ошибки связанные с:
   - Триггером `on_auth_user_created`
   - Функцией `handle_new_user()`
   - Таблицей `profiles`
   - Таблицей `event_participants`
4. Если найдены ошибки, скопируйте их текст для анализа

#### Проверка RLS политик

1. Выполните запрос для проверки политик таблицы `profiles`:
   ```sql
   SELECT
     schemaname,
     tablename,
     policyname,
     permissive,
     roles,
     cmd,
     qual,
     with_check
   FROM pg_policies
   WHERE tablename = 'profiles'
   ORDER BY cmd;
   ```

2. Проверьте наличие следующих политик:
   - **SELECT**: "Profiles are viewable by everyone" (qual = true)
   - **INSERT**: "Пользователи могут создавать свой профиль" (with_check = auth.uid() = id)
   - **UPDATE**: "Users can update own profile" (qual = auth.uid() = id)

3. Если политики отсутствуют или некорректны, обратитесь к файлу `schema.sql` для их восстановления

#### Проверка триггера

1. Выполните запрос для проверки существования триггера:
   ```sql
   SELECT
     trigger_name,
     event_manipulation,
     event_object_table,
     action_statement,
     action_timing
   FROM information_schema.triggers
   WHERE trigger_name = 'on_auth_user_created';
   ```

2. Ожидаемый результат:
   - `trigger_name`: on_auth_user_created
   - `event_manipulation`: INSERT
   - `event_object_table`: users
   - `action_statement`: EXECUTE FUNCTION handle_new_user()
   - `action_timing`: AFTER

3. Выполните запрос для проверки кода функции:
   ```sql
   SELECT pg_get_functiondef('public.handle_new_user'::regproc);
   ```

4. Проверьте что функция:
   - Использует `IF NOT EXISTS` для проверки существования профиля
   - Включает колонку `role` в INSERT
   - НЕ содержит блок `EXCEPTION WHEN OTHERS`

---

### Шаблон документирования результатов

**Дата тестирования**: [УКАЖИТЕ ДАТУ]
**Тестировщик**: [ВАШЕ ИМЯ]
**Версия приложения**: [COMMIT HASH или VERSION]

#### Тест 1: Регистрация нового пользователя
- Статус: ✅ ПРОЙДЕН / ❌ ПРОВАЛЕН
- Email тестового пользователя: _______________
- Профиль создан: ДА / НЕТ
- Колонка `role` заполнена: ДА / НЕТ
- Значение `role`: _______________
- Ошибки в консоли: ДА / НЕТ
- **Примечания**: _______________

#### Тест 2: Страница профиля (пользователь acd70956...)
- Статус: ✅ ПРОЙДЕН / ❌ ПРОВАЛЕН
- Страница загружается: ДА / НЕТ
- Статус HTTP запроса: _______________
- Ошибки 406 в Network: ДА / НЕТ
- Ошибки в Console: ДА / НЕТ
- Компонент GameCard отображается: ДА / НЕТ
- **Примечания**: _______________

#### Тест 3: Присоединение к событию
- Статус: ✅ ПРОЙДЕН / ❌ ПРОВАЛЕН
- ID события: _______________
- Присоединение успешно: ДА / НЕТ
- Статус HTTP запроса: _______________
- Ошибки 409 в Network: ДА / НЕТ
- Ошибки FK constraint: ДА / НЕТ
- Пользователь в списке участников: ДА / НЕТ
- **Примечания**: _______________

#### Тест 4: Доступ к админ-панели
- Статус: ✅ ПРОЙДЕН / ❌ ПРОВАЛЕН

**Модератор**:
- Email: _______________
- Доступ получен: ДА / НЕТ
- Ошибки 406: ДА / НЕТ
- Вкладки отображаются: ДА / НЕТ
- Пункт "Админ" в навигации: ДА / НЕТ

**Обычный пользователь**:
- Email: _______________
- Редирект на главную: ДА / НЕТ
- Ошибки 406: ДА / НЕТ
- Пункт "Админ" в навигации: ДА / НЕТ

- **Примечания**: _______________

#### Тест 5: Целостность данных
- Статус: ✅ ПРОЙДЕН / ❌ ПРОВАЛЕН
- Всего пользователей в системе: _______________
- Пользователей без профилей: _______________
- Профилей без роли: _______________
- Пользователь acd70956... имеет профиль: ДА / НЕТ
- **Примечания**: _______________

#### Дополнительные проверки
- Логи Supabase проверены: ДА / НЕТ
- Найдены ошибки в логах: ДА / НЕТ
- RLS политики проверены: ДА / НЕТ
- Триггер проверен: ДА / НЕТ
- **Примечания**: _______________

---

### Общий вывод тестирования

**Статус**: ✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ / ⚠️ ЧАСТИЧНО ПРОЙДЕНЫ / ❌ ТЕСТЫ ПРОВАЛЕНЫ

**Краткое резюме**:
[Опишите общее состояние системы после тестирования]

**Обнаруженные проблемы**:
1. [Проблема 1]
2. [Проблема 2]
...

**Исправленные проблемы**:
1. [Проблема 1 и как исправлена]
2. [Проблема 2 и как исправлена]
...

**Рекомендации**:
1. [Рекомендация 1]
2. [Рекомендация 2]
...

**Следующие шаги**:
1. [Шаг 1]
2. [Шаг 2]
...

---

### Критерии успешного тестирования

Тестирование считается **полностью успешным**, если:

- ✅ Тест 1 пройден: Новые пользователи регистрируются с профилями и `role='user'`
- ✅ Тест 2 пройден: Страница профиля загружается без 406 ошибок
- ✅ Тест 3 пройден: Присоединение к событиям работает без 409 ошибок
- ✅ Тест 4 пройден: Доступ к админ-панели работает корректно для модераторов и блокируется для обычных пользователей
- ✅ Тест 5 пройден: Все пользователи имеют профили, все профили имеют роли
- ✅ Нет ошибок в логах Supabase за последние 24 часа
- ✅ RLS политики настроены корректно
- ✅ Триггер `on_auth_user_created` работает корректно

Тестирование считается **частично успешным**, если:

- ⚠️ 4 из 5 основных тестов пройдены
- ⚠️ Найдены незначительные проблемы, не влияющие на основной функционал
- ⚠️ Найдены пользователи без профилей, но они успешно восстановлены

Тестирование считается **проваленным**, если:

- ❌ 3 или более основных тестов провалены
- ❌ Триггер не создаёт профили для новых пользователей
- ❌ Найдены критические ошибки в логах Supabase
- ❌ Множественные пользователи без профилей, которые не удаётся восстановить

---

### Важные замечания

1. **Очистка кэша**: Перед тестированием очистите кэш браузера (Ctrl+Shift+Delete) или используйте режим инкогнито
2. **Тестовые данные**: Не удаляйте тестовых пользователей до завершения всех тестов
3. **Скриншоты**: Сохраняйте скриншоты ошибок для последующего анализа
4. **Логи**: Регулярно проверяйте логи Supabase во время тестирования
5. **Бэкап**: Перед применением любых исправлений делайте бэкап базы данных
6. **Параллельное тестирование**: Тесты 1-4 можно выполнять параллельно в разных браузерах/сессиях
7. **Повторное тестирование**: После исправления проблем повторите все тесты заново
8. **Документирование**: Заполняйте шаблон документирования сразу после каждого теста

---

### Контрольный список перед тестированием

- [ ] Миграция `fix_user_creation_trigger.sql` применена
- [ ] Миграция `create_missing_profile.sql` применена
- [ ] Замена `.single()` на `.maybeSingle()` в `Profile.jsx` выполнена
- [ ] Замена `.single()` на `.maybeSingle()` в `Admin.jsx` выполнена
- [ ] Замена `.single()` на `.maybeSingle()` в `TopNav.jsx` выполнена
- [ ] Замена `.single()` на `.maybeSingle()` в `ProtectedAdminRoute.jsx` выполнена
- [ ] Доступ к Supabase Dashboard получен
- [ ] Доступ к учётной записи `andrei_999@list.ru` получен
- [ ] Создан тестовый пользователь с ролью `moderator`
- [ ] Браузер с DevTools готов к тестированию
- [ ] Шаблон документирования подготовлен

---

### Контакты для поддержки

Если в процессе тестирования возникли вопросы или проблемы:

1. Проверьте раздел "Действия при провале" для соответствующего теста
2. Проверьте логи Supabase: Dashboard → Logs → Database
3. Проверьте документацию в файле `INSTRUKTSIYA_ISPRAVLENIE_OSHIBKI_REGISTRATSII.md`
4. Обратитесь к разработчикам с детальным описанием проблемы и скриншотами